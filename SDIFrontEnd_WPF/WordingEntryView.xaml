<UserControl x:Class="SDIFrontEnd_WPF.WordingEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Height="450" Width="800">
    <Grid Background="LightBlue">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Text="Wording Entry" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="1" Grid.Row="0"/>
        
        <!-- Type, ID and Select -->
        <DockPanel Grid.Row="1" LastChildFill="True">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Margin="1">
                <Run d:Text="WordingType" Text="{Binding WordingType}"/>
                <Run Text="# "/>
                <Run d:Text="0" Text="{Binding CurrentWording.WordID}"/>
            </TextBlock>

            <Button HorizontalAlignment="Stretch" Content="Select Wording" Height="30" Margin="20,5,0,5" Command="{Binding SelectWordingCommand}"/>
            
        </DockPanel>
        <DockPanel Grid.Row="2">
            <Button DockPanel.Dock="Top" Content="Edit" Height="30" Width="40" HorizontalAlignment="Right" />
            <TextBox Text="{Binding CurrentWording.WordingText, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5,5,0,5" Grid.Row="2" />
        </DockPanel>
        
        <!-- Commands -->
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="3, 30, 3, 3" >
            <Button Content="Copy to New" Command="{Binding CopyToNewCommand}" CommandParameter="{Binding CurrentWording}" Height="20" Width="80" Margin="0,0,0,2"/>
            <Button Content="Add" Command="{Binding NewWordingCommand}" Height="20" Width="80" Margin="0,0,0,2"/>
            <Button Content="Delete" Command="{Binding DeleteWordingCommand}" CommandParameter="{Binding CurrentWording}" Height="20" Width="80" Margin="0,0,0,2"/>
            <Button Content="Clip Search" Command="{Binding ClipSearchCommand}" Height="20" Width="80"/>
            <Button Content="Text Search" Command="{Binding TextSearchCommand}" Height="20" Width="80"/>
        </StackPanel>
        
        <!-- Wording Usage list -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
            <Label Content="This wording appears in the following places" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>
            <DataGrid Margin="3" ItemsSource="{Binding Usages}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="VarName" Binding="{Binding VarName}" Width="Auto"/>
                    <DataGridTextColumn Header="Survey" Binding="{Binding SurveyCode}" Width="*"/>
                    <DataGridTextColumn Header="W#" Binding="{Binding WordID}" Width="2*"/>
                    <DataGridTextColumn Header="VarLabel" Binding="{Binding VarLabel}" Width="2*"/>
                    <DataGridTextColumn Header="Locked" Binding="{Binding Locked}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        
        <!-- Navigation buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="5" Orientation="Horizontal" >
            <Button Content="&lt;&lt;&lt;" Command="{Binding PreviousItemCommand}" Width="50" Margin="5,0"/>
            <TextBlock d:Text="0 of 0" Text="{Binding ItemPosition}"/>
            <Button Content="&gt;&gt;&gt;" Command="{Binding NextItemCommand}" Width="50" Margin="5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
