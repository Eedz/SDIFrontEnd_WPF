<UserControl x:Class="SDIFrontEnd_WPF.ResponseSetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="Response Entry" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="1" Grid.Row="0"/>
        <DockPanel Grid.Row="1">
            <TextBlock VerticalAlignment="Center" Margin="1">
                <Run d:Text="WordingType" Text="{Binding ResponseType}"/>
                <Run Text=": "/>
                <Run d:Text="0" Text="{Binding CurrentWording.RespSetName}"/>
            </TextBlock>

            <Button HorizontalAlignment="Stretch" Content="Select Response" Height="30" Margin="20,5,0,5" Command="{Binding SelectWordingCommand}"/>

        </DockPanel>
        <DockPanel Grid.Row="2">
            <Button DockPanel.Dock="Top" Content="Edit" Height="30" Width="40" HorizontalAlignment="Right" />
            <TextBox Text="{Binding CurrentWording.RespOptions, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5" Grid.Row="2" />
        </DockPanel>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="3, 30, 3, 3">
            <Button Content="Copy to New" Command="{Binding NewWordingCommand}" Height="20" Width="80" Margin="0,0,0,2"/>
            <Button Content="Add" Command="{Binding NewWordingCommand}" Height="20" Width="80" Margin="0,0,0,2"/>
            <Button Content="Delete" Command="{Binding SaveWordingCommand}" Height="20" Width="80" Margin="0,0,0,2"/>
            <Button Content="Clip Search" Command="{Binding DeleteWordingCommand}" Height="20" Width="80"/>
            <Button Content="Text Search" Command="{Binding DeleteWordingCommand}" Height="20" Width="80"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
            <Label Content="This wording appears in the following places" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>
            <DataGrid Margin="3" ItemsSource="{Binding WordingUsages}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="VarName" Binding="{Binding VarName}" Width="Auto"/>
                    <DataGridTextColumn Header="Survey" Binding="{Binding SurveyCode}" Width="AUto"/>
                    <DataGridTextColumn Header="W#" Binding="{Binding RespSetName}" Width="70"/>
                    <DataGridTextColumn Header="VarLabel" Binding="{Binding VarLabel}" Width="*"/>
                    <DataGridTextColumn Header="Locked" Binding="{Binding Locked}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="5" Orientation="Horizontal" >
            <Button Content="&lt;&lt;&lt;" Command="{Binding PreviousItemCommand}" Width="50" Margin="5,0"/>
            <TextBlock d:Text="0 of 0" Text="{Binding ItemPosition}"/>
            <Button Content="&gt;&gt;&gt;" Command="{Binding NextItemCommand}" Width="50" Margin="5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
