<UserControl x:Class="SDIFrontEnd_WPF.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <StackPanel HorizontalAlignment="Center" Margin="5" Orientation="Horizontal" >
        <Button Content="&lt;&lt;&lt;" 
                Command="{Binding FirstItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                Width="50" Margin="5,0"/>
        <Button Content="&lt;" 
                Command="{Binding PreviousItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                Width="50" Margin="5,0"/>
        <TextBlock d:Text="0 of 0" 
                Text="{Binding ItemPosition, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <Button Content="&gt;" Command="{Binding NextItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                Width="50" Margin="5,0"/>
        <Button Content="&gt;&gt;&gt;" Command="{Binding LastItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                Width="50" Margin="5,0"/>
    </StackPanel>
</UserControl>
