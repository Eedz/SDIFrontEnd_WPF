<UserControl x:Class="SDIFrontEnd_WPF.Views.ChangedQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             xmlns:converters ="clr-namespace:MvvmLib.Converters;assembly=MvvmLib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:HtmlToFlowDocumentConverter x:Key="HtmlToFlow" />
            <converters:StringToGridLengthConverter x:Key="GridLengthConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" >
                <ListBox Height="250" Width="90" ItemsSource="{Binding WordingList}" SelectedItem="{Binding SelectedWording}"
                        VerticalAlignment="Top" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding }" Margin="3" Height="25" TextAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
           
                <!-- question text -->
                <FlowDocumentScrollViewer IsToolBarVisible="False" VerticalScrollBarVisibility="Auto" MinHeight="200" Width="300" >
                    
                    <FlowDocumentScrollViewer.Document>
                        <Binding Path="CurrentQuestionText" Converter="{StaticResource HtmlToFlow}" />
                    </FlowDocumentScrollViewer.Document>
                </FlowDocumentScrollViewer>
            </StackPanel>
        
    </Grid>
</UserControl>
