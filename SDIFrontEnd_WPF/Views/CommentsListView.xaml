<UserControl x:Class="SDIFrontEnd_WPF.Views.CommentsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListBox Grid.Row="2" Grid.Column="2"  ItemsSource="{Binding Comments, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalContentAlignment="Stretch"
         ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="LightBlue">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Border BorderBrush="Gray" BorderThickness="1">
                    <DockPanel LastChildFill="False" Margin="4">
                        <TextBlock Text="{Binding Author.Name}" DockPanel.Dock="Right" Margin="8,0" Width="60"/>
                        <TextBlock Text="{Binding NoteType.TypeName}" DockPanel.Dock="Right" Margin="8,0" Width="100"/>
                        <TextBlock Text="{Binding NoteDate, StringFormat={}{0:yyyy-MM-dd}}" DockPanel.Dock="Right" Margin="8,0" Width="75"/>
                        <TextBlock Text="{Binding Notes.NoteText}" TextWrapping="Wrap" Margin="0,0" MaxWidth="250"/>
                    </DockPanel>
                </Border>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
