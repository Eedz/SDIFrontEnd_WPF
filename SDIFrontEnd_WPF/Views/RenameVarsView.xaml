<UserControl x:Class="SDIFrontEnd_WPF.Views.RenameVarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.35*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Text="Rename Variables" FontSize="20" Margin="5" Grid.Row="0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <GroupBox Header="Rename by...">
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content ="VarName" IsChecked="{Binding UseVarName}"/>
                    <RadioButton Content="refVarName"/>
                </StackPanel>
            </GroupBox>
            <GroupBox Header="VarName Source" Visibility="Hidden">
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content ="From File" IsChecked="{Binding UseFile}"/>
                    <RadioButton Content="Manual List"></RadioButton>
                </StackPanel>
            </GroupBox>
            <StackPanel>
                <TextBlock Text="Source File"></TextBlock>
                <TextBox Width="100" Height="40" Text="{Binding SourceFilePath}"/>
            </StackPanel>
            <StackPanel>
                <Button Content="Browse..." Command="{Binding BrowseForFileCommand}"/>
                <Button Content="Import List" Command="{Binding ImportRenamesCommand}"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Renames to perform"/>
            <DataGrid ItemsSource="{Binding RenameList}" Height="600" VerticalScrollBarVisibility="Auto" CanUserAddRows="True" CanUserDeleteRows="True"
                      AutoGenerateColumns="False" RowHeight="100">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Old VarName" Binding ="{Binding OldVarName}" Width="85"/>
                    <DataGridTextColumn Header="New VarName" Binding ="{Binding NewVarName}" Width="90"/>
                    <DataGridTemplateColumn Header="VarLabel" Width="200" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding VarLabel}" TextWrapping="Wrap" VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Surveys Affected" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                
                                    <TextBlock Text="{Binding SurveysAffected}" TextWrapping="Wrap" VerticalAlignment="Center" />
                                
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding SurveysAffected,
                                    Mode=TwoWay,
                                    UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     MaxHeight="150"
                                     Padding="4,2"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>


                    <DataGridTemplateColumn Header="Surveys With Both" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <TextBlock Text="{Binding SurveysBoth}" TextWrapping="Wrap" VerticalAlignment="Center" />

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Locked Surveys" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <TextBlock Text="{Binding SurveysLocked}" TextWrapping="Wrap" VerticalAlignment="Center" />

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <ContentControl Content="{Binding VarChangeDetails}" Height="200" Width="400"/>
            <StackPanel Grid.Row="0" Grid.Column="3" Grid.RowSpan="4">
                <CheckBox Content="Do No Document"/>
                <Button Content="Rename" Width="100" HorizontalAlignment="Right" Command="{Binding PerformRenamesCommand}"/>
                <Button Content="Unlock Surveys"/>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</UserControl>
