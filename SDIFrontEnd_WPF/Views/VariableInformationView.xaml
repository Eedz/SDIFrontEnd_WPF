<UserControl x:Class="SDIFrontEnd_WPF.Views.VariableInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="Root">
    <Grid>
        <StackPanel Height="800">
            <TextBlock Text="Variable Information" FontSize="26"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding FilterString, UpdateSourceTrigger=PropertyChanged}" d:Text="Filter..." Width="120" Margin="3"></TextBox>
                <CheckBox Content="Exact" IsChecked="{Binding ExactMatch}" Margin="3"/>
                <Button Content="Save All" Command="{Binding SaveAllCommand}" Margin="900,3,3,3"/>
            </StackPanel>
            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" Height="800">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Dirty}" Value="True">
                                <Setter Property="Background" Value="Orange"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="VarName" Binding="{Binding VarName}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="VarLabel" Binding="{Binding VarLabel, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTemplateColumn Header="Domain">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Domain.Label}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Domains, ElementName=Root}"
                                    SelectedItem="{Binding Domain, Mode=TwoWay}" DisplayMemberPath="Label"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- TOPIC -->
                    <DataGridTemplateColumn Header="Topic">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Topic.Label}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Topics, ElementName=Root}"
                                    SelectedItem="{Binding Topic, Mode=TwoWay}" DisplayMemberPath="Label"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- CONTENT -->
                    <DataGridTemplateColumn Header="Content">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Content.Label}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Contents, ElementName=Root}"
                                    SelectedItem="{Binding Content, Mode=TwoWay}" DisplayMemberPath="Label"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- PRODUCT -->
                    <DataGridTemplateColumn Header="Product">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Product.Label}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.Products, ElementName=Root}"
                                    SelectedItem="{Binding Product, Mode=TwoWay}" DisplayMemberPath="Label"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>



                    <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Save" Command="{Binding SaveCommand}"/>
                                <Button Content="Cancel" Command="{Binding CancelCommand}"/>
                                <Button Content="Delete" Command="{Binding DeleteCommand}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
