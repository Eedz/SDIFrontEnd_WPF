<UserControl x:Class="SDIFrontEnd_WPF.Views.TopicContentReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             xmlns:behaviors="clr-namespace:MvvmLib.Behaviors;assembly=MvvmLib"
             mc:Ignorable="d" 
             d:DesignHeight="330" d:DesignWidth="420" Height="400" Width="420">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="155"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"></RowDefinition>
            <RowDefinition Height="120"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
          
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Grid.ColumnSpan="3" Text="Topic Content Report" HorizontalAlignment="Left"/>

        <StackPanel  Grid.Row="1"  VerticalAlignment="Top" Margin="3">
            <TextBlock Text="Select Survey(s)" Margin="0,0,0,3"/>
            <StackPanel Orientation="Horizontal" Height="30" Margin="0,0,0,3">
                <ComboBox ItemsSource="{Binding SurveyList}"
                      SelectedItem="{Binding SelectedSurvey, Mode=TwoWay}"
                     
                      Margin="0,0,3,0" Width="80"/>
                <Button Width="30" Margin="0,0,3,0" Command="{Binding RemoveSurveyCommand}">
                    <Image Source="/Images/GlyphLeft.png"/>
                </Button>
                <Button Width="30" Margin="0,0,3,0" Command="{Binding AddSurveyCommand}">
                    <Image Source="/Images/GlyphRight.png"/>
                </Button>
            </StackPanel>
        </StackPanel>
        
        <ListBox Grid.Row="1" Grid.Column="1" Margin="3,20,3,3" ItemsSource="{Binding ReportSurveys}" SelectedItem="{Binding SelectedReportSurvey}" DisplayMemberPath="SurveyCode"></ListBox>

        <StackPanel Grid.Row="1" Grid.Column="3">
            <GroupBox Header="Labels to Include" Margin="2">
                <StackPanel>
                    <RadioButton Content="Topic/Content" IsChecked="True" />
                    <RadioButton Content="Topic/Content/Product" IsChecked="{Binding IncludeProduct}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="3">
            <TextBlock Text="Select Products"/>
            <ListBox ItemsSource="{Binding ProductList.Items}"
                     SelectionMode="Multiple" Height="185">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="1" Margin="3">
            <TextBlock Text="Show Fields"/>
            <ListBox ItemsSource="{Binding FieldNames.Items}" 
                     SelectionMode="Multiple" Height="185">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="3" Orientation="Horizontal" Height="30">
            <Button Content="Generate" Command="{Binding GenerateReportCommand}" Width="80" Margin="10,0,10,0"/>
            <Button Command="{Binding OpenFolderCommand}">
                <Image Source="/Images/Refresh.png"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
