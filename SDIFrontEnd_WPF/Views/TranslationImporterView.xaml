<UserControl x:Class="SDIFrontEnd_WPF.Views.TranslationImporterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             xmlns:converters="clr-namespace:MvvmLib.Converters;assembly=MvvmLib"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="400" Height="550">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:HtmlToFlowDocumentConverter x:Key="HtmlToFlow" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="LightSkyBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"></ColumnDefinition>
            <!--<ColumnDefinition Width="300"></ColumnDefinition>-->
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="Translation Importer" FontSize="24" HorizontalAlignment="Center" Margin="10" Grid.ColumnSpan="2"/>
        <Border Grid.Row="1" Effect="{StaticResource SunkenEffect}">
            <StackPanel  Orientation="Horizontal" HorizontalAlignment="Center" >

            <StackPanel  Width="125">
                <TextBlock Text="Survey"></TextBlock>
                <ComboBox SelectedItem="{Binding SelectedSurvey}" ItemsSource="{Binding SurveyList}"></ComboBox>
            </StackPanel>
            <StackPanel Margin="15,0,0,0" Width="125">
                <TextBlock Text="Language"></TextBlock>
                <ComboBox SelectedItem="{Binding SelectedLanguage}" ItemsSource="{Binding LanguageList}"></ComboBox>
            </StackPanel>
        </StackPanel>
        </Border>
        <Border Grid.Row="2" Effect="{StaticResource SunkenEffect}">
            <StackPanel Margin="5" Orientation="Horizontal"  VerticalAlignment="Top">
            <TextBlock Text="Source File" VerticalAlignment="Center"/>
            <TextBox Text="{Binding SourceFile}" Margin="5" Width="180" Height="55" TextWrapping="Wrap"></TextBox>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Browse..." Margin="3" Width="60" Height="30" Command="{Binding BrowseForFileCommand}"/>
                    <Button Content="Import" Margin="3" Width="60" Height="30" Command="{Binding ImportCommand}"/>
                </StackPanel>
                
            </StackPanel>
        </StackPanel>
        </Border>

        <Border Grid.Row="3" Effect="{StaticResource SunkenEffect}">
            <TabControl  Height="350" Margin="5">
                <TabItem Header="Imported">
                    <StackPanel>
                        <TextBlock Text="{Binding ImportedCurrent.VarName}" />
                        <FlowDocumentScrollViewer Height="250" FontSize="10" Grid.Column="1" Grid.RowSpan="9" IsToolBarVisible="False" BorderBrush="LightGray" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" >
                            <FlowDocumentScrollViewer.Document>
                                <Binding Path="CurrentImportedText" Converter="{StaticResource HtmlToFlow}" />
                            </FlowDocumentScrollViewer.Document>
                        </FlowDocumentScrollViewer>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                            <Button Content="⟵ Previous"
                Command="{Binding ImportedPrevCommand}"
                Margin="5" />

                            <Button Content="Next ⟶"
                Command="{Binding ImportedNextCommand}"
                Margin="5" />
                        </StackPanel>
                    </StackPanel>
                </TabItem>
                <TabItem Header="Missing">
                    <StackPanel>
                        <TextBlock Text="{Binding MissingCurrent.VarName}" />
                        <FlowDocumentScrollViewer Height="250" FontSize="10" Grid.Column="1" Grid.RowSpan="9" IsToolBarVisible="False" BorderBrush="LightGray" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" >
                            <FlowDocumentScrollViewer.Document>
                                <Binding Path="CurrentMissingText" Converter="{StaticResource HtmlToFlow}" />
                            </FlowDocumentScrollViewer.Document>
                        </FlowDocumentScrollViewer>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                            <Button Content="⟵ Previous"
                            Command="{Binding MissingPrevCommand}"
                            Margin="5" />

                            <Button Content="Next ⟶"
                            Command="{Binding MissingNextCommand}"
                            Margin="5" />
                        </StackPanel>
                    </StackPanel>
                </TabItem>
                <TabItem Header="Duplicates">
                    <StackPanel>
                        <TextBlock Text="{Binding DuplicateCurrent.VarName}" />
                        <FlowDocumentScrollViewer Height="250" FontSize="10" Grid.Column="1" Grid.RowSpan="9" IsToolBarVisible="False" BorderBrush="LightGray" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10" >
                            <FlowDocumentScrollViewer.Document>
                                <Binding Path="CurrentDuplicateText" Converter="{StaticResource HtmlToFlow}" />
                            </FlowDocumentScrollViewer.Document>
                        </FlowDocumentScrollViewer>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                            <Button Content="⟵ Previous"
                Command="{Binding DuplicatePrevCommand}"
                Margin="5" />

                            <Button Content="Next ⟶"
                Command="{Binding DuplicateNextCommand}"
                Margin="5" />
                        </StackPanel>
                    </StackPanel>
                </TabItem>


            </TabControl>
        </Border>

        <Border Grid.Row="4" Effect="{StaticResource SunkenEffect}" >
            <Button Margin="50, 10" Content="Save to Database" Command="{Binding SaveCommand}"/>
        </Border>
    </Grid>
</UserControl>
