<UserControl x:Class="SDIFrontEnd_WPF.Views.QuestionHistoryManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="150"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="Question History" FontSize="24" HorizontalAlignment="Center"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBlock Text="Survey" Margin="4"/>
            <ComboBox ItemsSource="{Binding SurveyList}" Width="100" SelectedItem="{Binding SelectedSurvey}"/>
            <TextBlock Text="VarName" Margin="4"/>
            <ComboBox ItemsSource="{Binding VarNameList}" Width="100" SelectedItem="{Binding SelectedVarName}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal">

            
                <!--<StackPanel Orientation="Horizontal" >-->
                    <!--<ListBox Grid.Row="2" Height="276" Width="150" ItemsSource="{Binding WordingList}" SelectedItem="{Binding SelectedWording}"
                         VerticalAlignment="Top" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Hidden">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding }" Margin="3" Height="40" TextAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>-->
                    <!--<ContentControl Content="{Binding QuestionHistoryVM}"/>-->
            <StackPanel CanHorizontallyScroll="True" Width="900">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Padding="10">
                <ItemsControl ItemsSource="{Binding QuestionHistoryList}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="FrameworkElement.Margin" Value="8"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Effect="{StaticResource SunkenEffect}"  Width="400"  Height="400" CornerRadius="8" Background="White" BorderBrush="#DDD" BorderThickness="1" Padding="12">
                                <ContentPresenter Content="{Binding}" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </ScrollViewer>
            </StackPanel>
            <!--</StackPanel>-->
            
            
            <!-- Wording History pane-->
            <Border Width="200">
                <ListBox ItemsSource="{Binding WordingHistoryVMs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl Content="{Binding }"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ListBox>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <local:CommentsListView Comments="{Binding Comments}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
