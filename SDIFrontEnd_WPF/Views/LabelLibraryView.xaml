<UserControl x:Class="SDIFrontEnd_WPF.Views.LabelLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="45"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            
            <ColumnDefinition Width="275"></ColumnDefinition>
            <ColumnDefinition Width="100"></ColumnDefinition>
            <ColumnDefinition Width="500"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Label Library" FontWeight="Bold" FontSize="30" Grid.ColumnSpan="2"/>


       
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Height="30" DockPanel.Dock="Top" HorizontalAlignment="Center">
            
        </StackPanel>

        <TextBlock Text="VarNames with this label" Margin="5" DockPanel.Dock="Top" HorizontalAlignment="Center" TextAlignment="Center" FontWeight="Bold" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap"/>

        <TextBlock Text="Questions with this VarName" FontWeight="Bold" DockPanel.Dock="Top" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Grid.Column="2" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="1" Margin="5" Orientation="Horizontal" Grid.ColumnSpan="2">
            <TextBlock Text="Label Type" Margin="5" HorizontalAlignment="Center" Grid.Row="1" FontSize="16"/>
            <ComboBox ItemsSource="{Binding LabelTypes}" SelectedItem="{Binding SelectedLabelType}" Margin="4" Width="80" FontSize="16"/>
            <Button Margin="3,3,20,3" Width="20">
                <Image Source="/Images/add.png"/>
            </Button>
            <Button Margin="30,3,3,3" Width="20">
                <Image Source="/Images/delete.png"/>
            </Button>
        </StackPanel>

        <Border Grid.Column="0" Grid.Row="2" Margin="5">
            <DockPanel>
                
                <DataGrid ItemsSource="{Binding Labels}" SelectedItem="{Binding SelectedLabel}" SelectionMode="Single"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto"/>
                        <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Grid.Row="2" Margin="5">
            <DockPanel>
                <DataGrid ItemsSource="{Binding VarNames}" SelectedItem="{Binding SelectedVarName}"
                          SelectionMode="Single"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="VarName" Binding="{Binding VarName}" Width="*"/>
                    
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Border Grid.Column="2" Grid.Row="2" Margin="5">
            <DataGrid ItemsSource="{Binding Questions}"
                      SelectionMode="Single"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      >
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Survey" Binding="{Binding SurveyCode}" Width="100"/>
                    <DataGridTextColumn Header="VarName" Binding="{Binding VarName.VarName}" Width="80"/>
                    <DataGridTextColumn Header="Question" Binding="{Binding QuestionText}" Width="*" >
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
