<UserControl x:Class="SDIFrontEnd_WPF.Views.ResponseSetItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             xmlns:mvvm="clr-namespace:MvvmLib;assembly=MvvmLib"
             xmlns:converters="clr-namespace:MvvmLib.Converters;assembly=MvvmLib"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGoldenrodYellow">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="5"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <!-- Commands -->
        <DockPanel Grid.ColumnSpan="2">

            <Button Command="{Binding SaveCommand}" Content="Save" Margin="3" Height="20" Width="35"/>
            <Button Command="{Binding ToggleEditCommand}" Content="Edit" Margin="3" Height="20" Width="35"/>

            <Button Command="{Binding DeleteCommand}" Content="Delete" HorizontalAlignment="Right" Margin="3" Height="20" Width="45"/>

        </DockPanel>
        <!-- Wording Text -->
        <RichTextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="3,0,0,0" 
                VerticalScrollBarVisibility="Visible" AcceptsTab="True" IsDocumentEnabled="True"
                mvvm:RichTextBoxHelper.Document="{Binding ResponseText, Mode=TwoWay}" IsReadOnly="{Binding LockedForEditing}">
            <xctk:RichTextBoxFormatBarManager.FormatBar>
                <xctk:RichTextBoxFormatBar/>
            </xctk:RichTextBoxFormatBarManager.FormatBar>
        </RichTextBox>

        <!-- Splitter -->
        <GridSplitter Grid.Row="2" Grid.ColumnSpan="2"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Center"
              Height="5"
              Background="Gray"
              ShowsPreview="True" />

        <!-- Wording Usage list -->
        <DockPanel Grid.Row="3" Grid.ColumnSpan="2">
            <Label DockPanel.Dock="Top" Content="This response set appears in the following places" FontWeight="Bold" HorizontalAlignment="Center" Margin="3"/>
            <DataGrid Margin="3" ItemsSource="{Binding Usages}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="VarName" Binding="{Binding VarName}" Width="Auto"/>
                    <DataGridTextColumn Header="Survey" Binding="{Binding SurveyCode}" Width="Auto"/>
                    <DataGridTextColumn Header="W#" Binding="{Binding RespName}" Width="70"/>
                    <DataGridTextColumn Header="VarLabel" Binding="{Binding VarLabel}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Locked" Binding="{Binding Locked}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
