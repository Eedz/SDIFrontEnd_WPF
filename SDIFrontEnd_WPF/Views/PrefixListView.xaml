<UserControl x:Class="SDIFrontEnd_WPF.Views.PrefixListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Prefix list -->
        <StackPanel Grid.Column="0">

            <Button Content="Add" Command="{Binding AddPrefixCommand}" />
            <Button Content="Delete" Command="{Binding DeletePrefixCommand}" Margin="0,5,0,10"/>

            <ListBox ItemsSource="{Binding Records}"
                     SelectedItem="{Binding SelectedRecord}"
                     DisplayMemberPath="Item.Prefix"/>
        </StackPanel>

        <!-- RIGHT: Details -->
        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Fields -->
            <StackPanel Orientation="Vertical">

                <TextBox Text="{Binding SelectedRecord.Prefix, UpdateSourceTrigger=PropertyChanged}" d:Text="Prefix"/>
                <TextBox Text="{Binding SelectedRecord.PrefixName, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Text="{Binding SelectedRecord.ProductType, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Text="{Binding SelectedRecord.Description, UpdateSourceTrigger=PropertyChanged}" />

                <CheckBox Content="Inactive"
                          IsChecked="{Binding SelectedRecord.Inactive}"/>

                <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Ranges -->
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding SelectedRecord.Item.Ranges}"
                      SelectedItem="{Binding SelectedRange}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Lower" Binding="{Binding Lower}" />
                    <DataGridTextColumn Header="Upper" Binding="{Binding Upper}" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- Usages -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding Usages}"
                      AutoGenerateColumns="True"/>
        </Grid>
    </Grid>
</UserControl>
