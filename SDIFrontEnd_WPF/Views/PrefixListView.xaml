<UserControl x:Class="SDIFrontEnd_WPF.Views.PrefixListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDIFrontEnd_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="Prefix List" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>

        <!-- Prefix list -->
        <DockPanel Grid.Column="0" Grid.Row="1">

            <Button Content="Add" Command="{Binding AddPrefixCommand}" DockPanel.Dock="Top"/>
            <Button Content="Delete" Command="{Binding DeletePrefixCommand}" Margin="0,5,0,10" DockPanel.Dock="Top"/>
            <TextBlock Text="Prefixes" FontWeight="Bold" Margin="3" DockPanel.Dock="Top"/>
            <ListBox ItemsSource="{Binding Records}"
                     SelectedItem="{Binding SelectedRecord}"
                     DisplayMemberPath="Prefix"/>
        </DockPanel>

        <!-- Details -->
        <Grid Grid.Column="1" Grid.Row="1" Margin="10,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.25*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>

            <!-- Fields -->
            <Border >
                <StackPanel Orientation="Vertical" Width="300" Background="White" HorizontalAlignment="Left" Effect="{StaticResource SunkenEffect}">
                    <TextBlock Margin="2" >
                        <Run Text="ID: "/>
                        <Run Text="{Binding SelectedRecord.ID, Mode=OneWay}"/>
                    </TextBlock>
                    <TextBox Margin="2" Text="{Binding SelectedRecord.Prefix, UpdateSourceTrigger=PropertyChanged}" d:Text="Prefix"/>
                    <TextBox Margin="2" Text="{Binding SelectedRecord.PrefixName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Margin="2" Text="{Binding SelectedRecord.ProductType, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Height="60" Margin="2" Text="{Binding SelectedRecord.Description, UpdateSourceTrigger=PropertyChanged}" />
                    <CheckBox Margin="2" Content="Inactive" IsChecked="{Binding SelectedRecord.Inactive}"/>

                    <Button  Content="Save" Command="{Binding SaveCommand}" Margin="0,5,2,5" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
            <!--<StackPanel Grid.Column="2" >
                <Button Content="Print" Command="{Binding PrintCommand}"/>
                <CheckBox Content="Include Ranges" IsChecked="{Binding IncludeRangesInReport}"/>
            </StackPanel>-->
            
            <!-- Ranges -->
            <DockPanel Grid.Row="1" Grid.ColumnSpan="2"  Margin="5">
                <StackPanel Margin="5" DockPanel.Dock="Left">
                
                    <CheckBox IsChecked="{Binding FilterOnRange}" Content="Filter On" Height="30" HorizontalAlignment="Right" 
                              VerticalContentAlignment="Center" Margin="0,0,5,0"/>
             
                    <DataGrid Width="325" ItemsSource="{Binding SelectedRecord.Ranges}"
                          SelectedItem="{Binding SelectedRange}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="True">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Lower" Binding="{Binding Lower}" />
                            <DataGridTextColumn Header="Upper" Binding="{Binding Upper}" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
                <!-- Usages -->
                <DockPanel Margin="5" >
                    <Button DockPanel.Dock="Top" Command="{Binding ShowWordingsCommand}" Content="Show Wordings" Height="30" Width="100" HorizontalAlignment="Left"/>
                    <DataGrid Grid.Row="2"
                          ItemsSource="{Binding FilteredUsages}"
                              SelectedItem="{Binding SelectedUsage}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.VirtualizationMode="Recycling">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="refVarName" Binding="{Binding RefVarName}" />
                            <DataGridTextColumn Header="VarLabel" Binding="{Binding VarLabel}" />
                            <DataGridTextColumn Header="Topic" Binding="{Binding TopicLabel.Label}" />
                            <DataGridTextColumn Header="Content" Binding="{Binding ContentLabel.Label}" />
                            <DataGridTextColumn Header="Product" Binding="{Binding ProductLabel.Label}" />
                            <DataGridTextColumn Header="Domain" Binding="{Binding DomainLabel.Label}" />
                            <DataGridTextColumn Header="Surveys" Binding="{Binding SurveyList}" Width="*" >
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
